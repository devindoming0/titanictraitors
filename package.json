{
  "name": "titanictraitors",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix app && npm run build --prefix app",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": "20"
  }
}
